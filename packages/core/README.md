# @voice-line/core

Domain model for voice-line: types, interfaces, pipeline processors, and the `Session` state machine.

Everything else in the monorepo depends on this package. Leaf packages implement the interfaces defined here — never the reverse.
